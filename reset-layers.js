function resetLayers(){
    // get rid of the existing pin
    pinLayer.clearLayers();
    // get rid of the existing markers
    searchResultLayer.clearLayers();
    // get rid of the existing pin search radius
    pinRadiusLayer.clearLayers();
    return
}